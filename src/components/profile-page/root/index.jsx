import React from 'react';
import { UILayoutRoot } from '../../ui-layout/root';

export const text = {
  header: 'Profile Page',
};

export const ProfilePage = () => (
  <UILayoutRoot>
    <h1>{text.header}</h1>
  </UILayoutRoot>
);
